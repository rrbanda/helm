nameOverride: "backstage"

gitops:
  namespace:

helm:
  repo: https://charts.openshift.io
  chart: redhat-developer-hub
  version: 1.4.1
  values:
    git:
      host: 
      group: rhdh
      repo: developer-hub-config
      revision: main
      path: values.yaml
